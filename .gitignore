node_modules/

# macOS
.DS_Store

# Logs (never commit runtime / debug logs — may contain dictated text or PII)
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vp-connect*.log

# Local scratch
*.tmp
/tmp/

# Built native helper binary — produced by vendor/macos/build.sh and
# regenerated by `npm run build:helper` / `prepublishOnly`. The npm
# tarball includes it (see `files` in package.json), but git doesn't
# track build artifacts.
vendor/macos/vp-helper
